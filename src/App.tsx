import React from 'react';
import { BrowserRouter as Router, Route, Switch } from 'react-router-dom';

const App:React.FC = () => {
  return(
    <Router>
      <switch>
          <Route>
            
          </Route>
      </switch>
    </Router>
  );
};

export default App;
